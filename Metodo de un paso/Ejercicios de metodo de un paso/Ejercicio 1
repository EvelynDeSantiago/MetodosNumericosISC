using System;

class MetodoUnPaso
{
    static double Funcion(double x, double y)
    {
        return x + y;
    }

    static void Main()
    {
        double x = 0;
        double y = 1;
        double h = 0.1;

        for (int i = 0; i < 5; i++)
        {
            y = y + h * Funcion(x, y);
            x = x + h;

            Console.WriteLine("x = " + x + " y = " + y);
        }
    }
}

------------------------------Salida en consola-----------------------------

x = 0.1 y = 1.1
x = 0.2 y = 1.22
x = 0.3 y = 1.362
x = 0.4 y = 1.5282
x = 0.5 y = 1.72102