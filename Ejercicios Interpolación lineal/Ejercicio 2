using System;

class InterpolacionLineal
{
    static double Interpolar(double x0, double y0,
                              double x1, double y1,
                              double x)
    {
        return y0 + ((y1 - y0) / (x1 - x0)) * (x - x0);
    }

    static void Main()
    {
        double resultado = Interpolar(2, 4, 6, 12, 4);

        Console.WriteLine("Valor interpolado: " + resultado);
    }
}

-------------Valor interpolado: 8