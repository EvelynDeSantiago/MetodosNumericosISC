using System;

class Lagrange
{
    static double Interpolar(double[] x, double[] y, double valor)
    {
        double resultado = 0;
        int n = x.Length;

        for (int i = 0; i < n; i++)
        {
            double termino = y[i];

            for (int j = 0; j < n; j++)
            {
                if (j != i)
                {
                    termino *= (valor - x[j]) / (x[i] - x[j]);
                }
            }

            resultado += termino;
        }

        return resultado;
    }

    static void Main()
    {
        double[] x = { 0, 1, 2 };
        double[] y = { 0, 1, 4 };

        double resultado = Interpolar(x, y, 1.5);

        Console.WriteLine("Valor interpolado: " + resultado);
    }
}

-----------------Valor interpolado: 2.25