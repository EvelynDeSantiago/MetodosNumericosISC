using System;

class Ejercicio3
{
    static void Main(string[] args)
    {
        double[][] matrix =
        {
            new double[] {3, 2, -4, 3},
            new double[] {2, 3, 3, 15},
            new double[] {5, -3, 1, 14}
        };

        GaussJordan(matrix);

        Console.WriteLine("---------Resultado del sistema de ecuaciones:");
        Console.WriteLine("La matriz en su forma escalonada reducida por filas es:");

        Imprimir(matrix);
    }

    static void GaussJordan(double[][] matrix)
    {
        int rows = matrix.Length;
        int cols = matrix[0].Length;

        for (int k = 0; k < rows; k++)
        {
            double divisor = matrix[k][k];

            for (int j = 0; j < cols; j++)
            {
                matrix[k][j] /= divisor;
            }

            for (int i = 0; i < rows; i++)
            {
                if (i != k)
                {
                    double factor = matrix[i][k];

                    for (int j = 0; j < cols; j++)
                    {
                        matrix[i][j] -= factor * matrix[k][j];
                    }
                }
            }
        }
    }

    static void Imprimir(double[][] matrix)
    {
        for (int i = 0; i < matrix.Length; i++)
        {
            for (int j = 0; j < matrix[0].Length; j++)
            {
                Console.Write(matrix[i][j] + "\t");
            }
            Console.WriteLine();
        }
    }
}

---------Resultado del sistema de ecuaciones:
La matriz en su forma escalonada reducida por filas es:
1	0	0	3
0	1	0	1
0	0	1	2